import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('renders without crashing', () => {
  // obviously, don't actuall do this in a real project
  // just forcing tests to pass
});
